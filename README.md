
# MuSuBi（結び）

> **検索しない音楽体験**
> 共感でつながる音楽コミュニティ

---


### MuSuBiは、
最初から商業化を前提にした大規模サービスではありません。
本プロジェクトの目的は、
再生数・知名度に依存しない音楽体験の設計
音楽を「探す」よりも「偶然出会う」体験の実装
音楽を作る・共有する人の視点を重視したプラットフォーム構築を、
実際のWebアプリケーションとして形にすることでした。
そのため、短期的な広告収益やPV最大化よりも、
思想とユーザー体験を優先した設計を行っています。
## 商業的な拡張可能性について
本サービスは将来的な商業展開を否定した設計ではありません。
以下のような拡張を想定しています。
アーティストプロフィール・ポートフォリオ機能
ライブ・イベント情報との連携
共感履歴を活用したキュレーション・推薦機能
インディーズアーティスト支援を目的としたB2B活用
本プロジェクトでは、
「まず体験と設計思想を作る」段階として実装しています。
未実装・今後の改善予定
以下は意図的に未実装、または今後検討している機能です。
検索機能・ジャンル分類
（思想とのバランスを考慮しつつ、UX向上のため検討）
プレイリスト・個人アルバム作成
ユーザーの音楽活動履歴の可視化
音楽活動者向けのプロフィールガイド機能
ライブ配信・過去視聴履歴機能
セキュリティ・例外処理・テストコードの強化
これらを通じて、
より実運用を意識したサービスへ発展させる予定です。
このプロジェクトで伝えたいこと
技術だけでなく「なぜ作るか」を考える姿勢
個人のバックグラウンドをサービス設計に落とし込む力
商業性と思想のバランスを意識したWebアプリ開発

---

##  開発背景（Why）

私は音楽を専攻しており、
現在の音楽市場における

* 再生数・ランキング至上主義
* ストリーミング数操作などの問題
* 「検索ありき」の受動的な音楽体験

に違和感を持っていました。

> **音楽は探すものだけではなく、
> 偶然出会い、共感する体験でも良いのではないか**

この考えを形にするため、
MuSuBiでは**あえて検索機能を実装しない**という設計判断を行いました。

---

## 検索機能を実装しなかった理由

一般的なWebサービスでは検索機能が必須ですが、
MuSuBiでは以下を優先しました。

* すでに知っている音楽を探すこと
* 他人の共感を通じて新しい音楽に出会うこと

その代替として

* 最新投稿一覧
* 共感数を基準としたランキング
* ランダム再生機能

を中心に設計しています。

これは機能不足ではなく、
**サービスの思想を優先した意図的な設計**です。

---

##  サービス名の意味

**MuSuBi（結び）**は、

* Music
* 結び（人と人をつなぐ）

を組み合わせた造語です。

音楽を通じて人が自然につながる場所、
その意味を込めて名付けました。

---

## 主な機能

###  音楽投稿

* YouTube URL投稿
* 音源ファイルアップロード
* YouTube動画IDを抽出し、サムネイル自動生成

###  共感（いいね）

* ログインユーザーのみ可能
* 単なる数値ではなく「誰が共感したか」を保持

### 共感ベースランキング

* 再生数ではなく共感数を基準
* 「人気」より「推薦」に近い概念

### コメント機能

* 音楽ごとのコメント投稿
* 音楽削除時にコメントも連動削除

###  権限管理

* USER / ADMIN ロール分離
* 管理者のみ削除操作が可能

---

##  設計・実装のポイント（コード抜粋）

### ① 共感を「数」ではなく「関係」として設計

共感（いいね）を
**Music と User の多対多関係**としてモデル化しました。

```java
@ManyToMany
Set<SiteUser> voter;
```

これにより
「何人にいいねされたか」ではなく
**「誰に共感された音楽か」**を表現できます。

将来的な拡張（推薦・履歴表示）も考慮した設計です。

---

### ② 共感数を基準としたランキング取得

サービスの思想を
データ取得ロジックにも反映しています。

```java
@Query(
  value = "SELECT * FROM music ORDER BY " +
          "(SELECT COUNT(*) FROM music_voter WHERE music_id = music.id) DESC LIMIT 10",
  nativeQuery = true
)
List<Music> findTopRanking();
```

---

### ③ 検索の代替としてのランダム再生

検索を行わず、
偶然の出会いを生むためのランダム再生機能です。

```java
public Long getRandomMusicId() {
    List<Music> musicList = this.musicRepository.findAll();
    if (musicList.isEmpty()) {
        return null;
    }
    Random random = new Random();
    return musicList.get(random.nextInt(musicList.size())).getId();
}
```

---

### ④ 音楽削除時のデータ整合性

音楽削除時に
関連コメントも自動削除されるよう設計しています。

```java
@OneToMany(mappedBy = "music", cascade = CascadeType.REMOVE)
private List<Answer> answerList;
```

---

### ⑤ 実運用を想定したセキュリティ設計

誤操作や悪意ある操作を防ぐため、
削除処理は管理者権限のみに制限しています。

```java
.requestMatchers("/admin/**").hasRole("ADMIN")
.requestMatchers("/music/delete/**").hasRole("ADMIN")
```

---

## 🛠 使用技術

### Backend

* Java 21
* Spring Boot 3.2
* Spring Security
* Spring Data JPA（Hibernate）

### Frontend

* Thymeleaf
* HTML / CSS / JavaScript
  （操作ボタンを最小限にしたUI設計）

### Database

* PostgreSQL

### その他

* Gradle
* Lombok

---

## 学んだこと・工夫した点

* Spring Securityによる認可設計
* 共感機能のドメイン設計
* 「なぜその機能を入れたか／入れなかったか」を説明できる設計

技術だけでなく、
**サービス思想とコードの一貫性**を意識しました。

---

## 今後の改善・拡張案

本プロジェクトでは、サービスの思想を優先し、
あえて実装していない機能があります。

- 検索機能・ジャンル選択  
  検索中心の体験に戻らないため現時点では未実装だが、
  ユーザー利便性とのバランスを考慮し今後検討予定。

- マイアルバム・プレイリスト機能  
  共感だけでなく、個人の音楽体験を蓄積できる仕組み。

- 音楽プロフィール（ポートフォリオ）機能  
  音楽活動を行うユーザーが自身の楽曲や活動を
  一つのページとして共有できる構想。

- ライブ・イベント共有機能  
  オンラインとオフラインの音楽体験をつなぐ拡張。

- 楽曲視聴履歴機能  
  検索ではなく「音楽体験の記録」としての履歴管理。

これらは「未完成」ではなく、
サービスを成長させるための設計上の余白として位置付けています。


##  今後の改善予定

* 例外処理（Global Exception Handler）
* ファイルアップロードのセキュリティ強化
* テストコード追加
* REST API構成への拡張検討

---

##  開発者

* 名前：**チョ・ヒョンファン（조현황）**
* 専攻：音楽
* GitHub：本リポジトリ参照

---
## プロジェクトの位置づけ（商業性について）



※ 本プロジェクトは学習・ポートフォリオ目的であり、
著作権を侵害する意図はありません。
商用化を行う場合は、権利申告・通報・削除フローの実装が必要だと考えています。
---



##  リポジトリ

👉 [https://github.com/whgusghkd777-debug/MuSuBiMusic](https://github.com/whgusghkd777-debug/MuSuBiMusic)

---

